# Database Configuration (solo para Docker Compose)
DB_NAME=odoo_db
DB_USER=odoo_user
DB_PASSWORD=your_secure_password_here
DB_HOST=db
DB_PORT=5432

# Odoo Configuration (solo para Docker Compose)
HTTP_PORT=8069
ADMIN_PASSWD=your_admin_password_here

# pgAdmin Configuration (solo para Docker Compose)
PGADMIN_DEFAULT_EMAIL=your_email@domain.es
PGADMIN_DEFAULT_PASSWORD=your_pgadmin_password_here